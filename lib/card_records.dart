import 'package:flutter/material.dart';

class CardRecords extends StatelessWidget {
  const CardRecords({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 40,
      width: 100,
      color: Colors.amber,
    );
  }
}
